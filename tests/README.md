Run a test file with `uv run pytest tests/test_data_model.py -v`

Run a single test within a file with `uv run pytest tests/test_complex_measures.py::TestComplexMeasures::test_sum_measure_with_pre_agg -v`
