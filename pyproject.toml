[project]
name = "datasubway"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "libcst>=1.8.6",
    "polars>=1.36.1",
    "pytest>=9.0.2",
]
